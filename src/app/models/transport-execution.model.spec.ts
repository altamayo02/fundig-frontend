import { TransportExecution } from './transport-execution.model';

describe('TransportExecution', () => {
  it('should create an instance', () => {
    expect(new TransportExecution()).toBeTruthy();
  });
});
