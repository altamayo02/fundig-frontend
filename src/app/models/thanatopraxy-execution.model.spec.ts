import { ThanatopraxyExecution } from './thanatopraxy-execution.model';

describe('ThanatopraxyExecution', () => {
  it('should create an instance', () => {
    expect(new ThanatopraxyExecution()).toBeTruthy();
  });
});
